{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build (one file)",
            "type": "shell",
            "command": "pyinstaller --noconsole --onefile --add-data='announcement.wav;.' productivity_suspension_system.py",
            "problemMatcher": [],
            "group": {
                "kind": "build"
            }
        },
        {
            "label": "build (one folder)",
            "type": "shell",
            "command": "pyinstaller --noconsole --onedir --add-data='announcement.wav;.' productivity_suspension_system.py",
            "problemMatcher": [],
            "group": {
                "kind": "build"
            }
        }
    ]
}